# Officer email recipient
OFFICER_EMAIL=sahilramteke95@gmail.com

# Optional SMTP Settings (leave blank for formatted local stdout/audit logging)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
FROM_EMAIL=alerts@rastarakshak.nagpur.gov.in

# Database (defaults to local sqlite:///./rastarakshak.db if empty)
DATABASE_URL=sqlite:///./rastarakshak.db
